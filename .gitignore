# Development dependencies and scripts
node_modules/

# Scratch and analysis scripts
scratch/
bbox.js
get_img_size.js
grid_detect.js
grid_detect2.js
grid_detect3.js
grid_detect4.js

# Archived/unused files (kept locally, not deployed)
waste/

# OS generated files
.DS_Store
Thumbs.db

.vercel

# Environment files. 'vercel env pull' writes the real REDIS_URL (password and all)
# into .env.production, so none of these may ever be committed.
.env
.env.*

# Debug screenshots and scratch output from assistant sessions
Claude outputs/
*.bak

# Packaged builds are produced by Android Studio / Xcode, not stored here
*.zip
*.apk
*.aab
*.ipa
